

var nums = [10, 20, 30, 40, 50, 60, 70];

// get element by index
console.log(nums[0]);

// update element by index
nums[1] = 100;

// find index of an element
// if does not exist indexOf return -1
var positionIndex = numbers.indexOf(89);
console.log(positionIndex);

